import 'package:flutter/material.dart';

class VideoPreviewView extends StatelessWidget {
  const VideoPreviewView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Remote Video Player'));
  }
}
